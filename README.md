

## System Architecture
The platform is architected with a decoupled frontend (React, Vite) and a stateless RESTful backend API (Node.js, Express, MongoDB). Requests are routed over secure HTTPS channels, utilizing token-based JWT credentials stored in browser contexts.